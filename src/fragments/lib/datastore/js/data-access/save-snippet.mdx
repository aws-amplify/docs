<BlockSwitcher>
<Block name="TypeScript">

```ts
await DataStore.save(
  new Post<Post>({
    title: 'My First Post',
    rating: 10,
    status: PostStatus.INACTIVE
  })
);
```

</Block>
<Block name="JavaScript">

```js
await DataStore.save(
  new Post({
    title: 'My First Post',
    rating: 10,
    status: PostStatus.INACTIVE
  })
);
```

</Block>
</BlockSwitcher>