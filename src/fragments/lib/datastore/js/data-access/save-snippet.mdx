<BlockSwitcher>
<Block name="TypeScript">

```ts
const post: Post = await DataStore.save(
  new Post<Post>({
    title: 'My First Post',
    rating: 10,
    status: PostStatus.INACTIVE
  })
);
```

</Block>
<Block name="JavaScript">

```js
const post = await DataStore.save(
  new Post({
    title: 'My First Post',
    rating: 10,
    status: PostStatus.INACTIVE
  })
);
```

</Block>
</BlockSwitcher>