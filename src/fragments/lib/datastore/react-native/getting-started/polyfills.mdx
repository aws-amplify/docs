### Add Required Polyfills

React Native or Expo apps using `react-native` 0.70 and above have Hermes enabled as the default engine and are missing some JavaScript standard library functionality that DataStore depends on. Add the following to the entry point of your app to polyfill these missing functionalities:

```js
import '@azure/core-asynciterator-polyfill';
```
