In order to retrieve all models that are related to a parent model, you can use query predicate with the query API as following:

```dart
List<Comment> comments = await Amplify.DataStore.query(
    Comment.classType,
    where: Comment.POST.eq('a-post-id'),
);
```