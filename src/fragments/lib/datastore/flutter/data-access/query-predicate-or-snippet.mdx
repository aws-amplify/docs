```dart
Future<void> fetchPublishedOrMoreThanFourRatingPosts() async {
  final posts = await Amplify.DataStore.query(
    Post.classType,
    where: Post.RATING.eq(2).or(Post.STATUS.eq(PostStatus.PUBLISHED)),
  );
  print('Posts: $posts');
}
```
