Alternatively, you can provide additional information about the user:

```js
const userInfo = {
  attributes: {
    hobbies: ['cooking', 'knitting']
  }
};

await Notifications.Push.identifyUser(userId, userInfo);
```
