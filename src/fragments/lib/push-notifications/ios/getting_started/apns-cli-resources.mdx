Choose _APNS_ when promoted:

```console
? Choose the push notification channel to enable.
❯ APNS |  Apple Push Notifications
  FCM  | » Firebase Push Notifications
  In-App Messaging
  Email
  SMS
```

Choose _Certificate_ when promoted:

```console
? Choose authentication method used for APNs
> Certificate
Key
```

The CLI will prompt for your _p12 certificate path_, enter it respectively.

Once all your configuration is complete run the following:

```sh
amplify push
```
