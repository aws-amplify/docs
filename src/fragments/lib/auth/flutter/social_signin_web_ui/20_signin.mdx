```dart
Future<void> signInWithSocialWebUI() async {
  try {
    final result = await Amplify.Auth.signInWithWebUI(
      provider: AuthProvider.facebook,
    );
    safePrint('Result: $result');
  } on AmplifyException catch (e) {
    safePrint(e.message);
  }
}
```
