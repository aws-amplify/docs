Configure Analytics by calling `Amplify.configure()` if you have not yet done so in your application. It's recommended that you add the Amplify configuration step to your app's root entry point. For example `index.ts` in React or `main.ts` in Angular.
