Amplify requires a minimum of API level 24 (Android 7.0) when targeting Android.

From your project root, navigate to the `android/app/` directory and open `build.gradle` in the text editor of your choice. Update the minimum Android SDK version to 24 or higher:

```bash
minSdkVersion 24
```