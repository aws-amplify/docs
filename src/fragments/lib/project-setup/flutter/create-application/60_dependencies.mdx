<BlockSwitcher>

<Block name="Stable">

   ```yaml
   environment:
     sdk: ">=2.15.0 <3.0.0"

   dependencies:
     flutter:
       sdk: flutter

     amplify_flutter: ^0.6.0
     amplify_auth_cognito: ^0.6.0
   ```

</Block>

<Block name = "Developer Preview">

   ```yaml
   environment:
     sdk: ">=2.15.0 <3.0.0"

   dependencies:
     flutter:
       sdk: flutter

     amplify_flutter: ^INSERT-DEV-PREVIEW-VERSION
     amplify_auth_cognito: ^INSERT-DEV-PREVIEW-VERSION
   ```

</Block>

</BlockSwitcher>