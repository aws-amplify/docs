Add the following dependency to your **app**'s build.gradle file and click "Sync Now" when prompted:

```groovy
dependencies {
    implementation 'com.amplifyframework:aws-auth-cognito:1.27.0'
    implementation 'com.amplifyframework:aws-geo-location:0.1.0'

    // Add this dependency to integrate MapLibre into your app
    implementation 'com.amplifyframework:maplibre-adapter:0.1.0'
}
```
