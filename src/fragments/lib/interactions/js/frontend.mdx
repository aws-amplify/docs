import js0 from '/src/fragments/lib/common/js/import_configuration.mdx';

Add the interactions package with npm:

```sh
npm install --save @aws-amplify/interactions aws-amplify

```

or with yarn:

```sh

yarn add @aws-amplify/interactions aws-amplify

```

<Fragments fragments={{  javascript: js0 , angular: js0, nextjs: js0, react: js0, vue: js0 }} />

```javascript
import { Amplify } from 'aws-amplify';
import amplifyconfig from './amplifyconfiguration.json';

Amplify.configure(amplifyconfig);
```
