## Configure remote configuration without configuration file
An alternative to configuring the `AWSCloudWAtchLoggingPlugin` is to specify the remote config provider as a parameter when constructing the `AWSCloudWatchLoggingPlugin` instance.
```
```

## Customizing AWS Credentials
By default, the `DefaultRemoteLoggingConstraintsProvider` uses Amplify Auth category as the AWS credentials provider for authorization access to the backend resources.
You can provide your own custom AWS credentials provider by overriding the default parameter.
```
```