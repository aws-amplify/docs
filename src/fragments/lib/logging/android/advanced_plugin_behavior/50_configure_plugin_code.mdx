## Configure plugin without configuration file
An alternative to configuring the `AWSCloudWAtchLoggingPlugin` with the `amplifyconfiguration_logging.json` is to specify the settings as a parameter when constructing the `AWSCloudWatchLoggingPlugin` instance.


TODO: ADD ANDROID CONTENT HERE