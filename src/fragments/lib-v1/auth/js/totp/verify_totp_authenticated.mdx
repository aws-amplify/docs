<BlockSwitcher>
<Block name="Typescript">

```ts

import {verifyTOTPSetup} from 'aws-amplify/auth';

async function handleVerifyTOTPSetup(totpCode: string) {
	try {
		await verifyTOTPSetup({ code: totpCode });
	} catch (error) {
		console.log(error);
	}
}

```

</Block>

<Block name="JavaScript">

```js

import {verifyTOTPSetup} from 'aws-amplify/auth';

async function handleVerifyTOTPSetup(totpCode) {
	try {
		await verifyTOTPSetup({ code: totpCode });
	} catch (error) {
		console.log(error);
	}
}

```

</Block>
</BlockSwitcher>