---
lastUpdated: '2021-12-04T23:07:15.000Z'
---
<Callout>

The `@hasOne` and `@hasMany` directives do not support referencing a model which then references the initial model via `@hasOne` or `@hasMany` if DataStore is enabled.

</Callout>
