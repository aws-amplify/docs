export const meta = {
  title: 'Sandbox features',
  description:
    'Sandbox features such as secrets, generate client code for mobile and x-plat'
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };

}

## Secrets

## Client config and codegen

## Name
