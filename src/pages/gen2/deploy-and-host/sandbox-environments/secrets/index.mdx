export const meta = {
  title: 'Working with secrets locally',
  description: 'Working with secrets in a cloud sandbox environment.'
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };

}
