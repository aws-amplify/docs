export const meta = {
  title: 'Sandbox deployments',
  description:
    'Learn how to use sandbox deployments.'
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };

}
