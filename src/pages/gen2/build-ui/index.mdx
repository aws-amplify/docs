export const meta = {
  title: 'Build UI',
  description: 'Learn how to build and customize your UI.'
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };

}

Amplify makes it easy to quickly build web app user interfaces using the UI component library, Figma-to-code generation, and CRUD form-generation capabilities. Learn more in the [UI Library.](https://ui.docs.amplify.aws/react/components)

![Screenshot showing Figma to Code](/images/gen2/how-amplify-works/ui.jpg)
