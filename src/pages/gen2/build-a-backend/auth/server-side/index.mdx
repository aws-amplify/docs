export const meta = {
  title: 'Server-side auth',
  description:
    'Learn how to use Amplify Auth on the server'
};

export function getStaticProps() {
  return {
    props: {
      meta
    }
  };
}
