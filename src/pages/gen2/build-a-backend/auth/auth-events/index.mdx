export const meta = {
  title: 'Listen to auth events',
  description:
    'Learn how to listen to auth events.'
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };

}
