export const meta = {
  title: "Extend with CDK",
  description: "Learn how to extend, modify, and override Amplify auth resources using AWS CDK",
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };
}
