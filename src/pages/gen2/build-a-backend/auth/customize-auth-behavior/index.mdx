export const meta = {
  title: "Customize auth behavior",
  description: "Learn how to customize the underlying authentication flows with custom business logic",
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };
}
