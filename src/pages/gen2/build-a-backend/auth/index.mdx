import { getCustomStaticPath } from '@/utils/getCustomStaticPath';
import { getChildPageNodes } from '@/utils/getChildPageNodes';
import directory from 'src/directory/directory.json';

export const meta = {
  title: 'Auth',
  description: 'Auth',
  route: '/gen2/build-a-backend/auth'
};
