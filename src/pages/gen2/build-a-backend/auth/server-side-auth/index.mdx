export const meta = {
  title: "Server-side auth",
  description: "Learn how to use Amplify Auth server-side in your applications",
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };
}
