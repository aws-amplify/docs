export const meta = {
  title: 'Bedrock response',
  description:
    'Leverage AI with Bedrock in a standalone Function'
};

export function getStaticProps() {
  return {
    props: {
      meta
    }
  };
}
