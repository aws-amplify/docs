export const meta = {
  title: 'Example: Email domain filtering',
  description:
    'Use an Auth Pre Signup trigger to allow or deny sign-ups based on email domains'
};

export function getStaticProps() {
  return {
    props: {
      meta
    }
  };
}
