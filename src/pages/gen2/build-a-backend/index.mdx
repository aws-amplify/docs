export const meta = {
  title: 'Build a backend overview',
  description:
    'Configure and customize authentication, data, storage, and functions to build a backend for your application.'
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };

}
