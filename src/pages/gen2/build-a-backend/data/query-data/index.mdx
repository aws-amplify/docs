export const meta = {
  title: 'Read application data',
  description:
    'Learn how to query application data.'
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };

}

# Read application data (queries)

## List data
### Apply filters in list queries
### Paginate over list queries
## Get a specific item
## Call a custom GraphQL query
