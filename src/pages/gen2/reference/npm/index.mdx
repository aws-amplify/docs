export const meta = {
  title: 'Overview',
  description:
    'Overview of available references for npm-aws-amplify-backend'
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };

}
