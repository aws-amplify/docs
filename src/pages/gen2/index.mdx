import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: `Gen 2`,
  description: `Gen 2 overview page`
};

export function getStaticProps(context) {
  return {
    props: {
      meta
    }
  };
}

# Gen 2

This page is just here for testing purposes.
