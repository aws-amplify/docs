import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Storing analytics data',
  description: 'The Amazon Kinesis Firehose analytics provider allows you to send analytics data to an Amazon Kinesis Firehose stream for reliably storing data.',
  platforms: [
    'javascript',
    'angular',
    'nextjs',
    'react',
    'vue'
  ],
  canonicalObjects: [
    {
      platforms: [
        'nextjs',
        'javascript',
        'vue',
        'angular',
        'react'
      ],
      canonicalPath: '/javascript/build-a-backend/more-features/analytics/storing-data/'
    }
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import js0 from '/src/fragments/lib/analytics/js/storing.mdx';

<Fragments
  fragments={{
    javascript: js0,
    angular: js0,
    nextjs: js0,
    react: js0,
    vue: js0
  }}
/>
