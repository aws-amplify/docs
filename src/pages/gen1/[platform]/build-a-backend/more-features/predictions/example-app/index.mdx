import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Example application',
  description: 'Sample code for using the Predictions category in Amplify.',
  platforms: [
    'javascript',
    'angular',
    'nextjs',
    'react',
    'vue'
  ],
  canonicalObjects: [
    {
      platforms: [
        'vue',
        'angular',
        'javascript',
        'nextjs',
        'react'
      ],
      canonicalPath: '/javascript/build-a-backend/more-features/predictions/example-app/'
    }
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import sampleJs from '/src/fragments/lib/predictions/js/sample.mdx';

<Fragments
  fragments={{
    javascript: sampleJs,
    angular: sampleJs,
    nextjs: sampleJs,
    react: sampleJs,
    vue: sampleJs
  }}
/>
