import { getApiStaticPath } from '@/utils/getApiStaticPath';

export const meta = {
  description: 'API Reference'
};

export const getStaticPaths = async () => {
  return getApiStaticPath();
};

export function getStaticProps(context) {
  return {
    props: {
      importName: context.params.importName,
      showBreadcrumbs: false,
      meta: {...meta, title: context.params.importName.join('/')}
    }
  };
}

<ReferencePage importName={props.importName.join('/')} />
