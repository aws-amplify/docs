import { getCustomStaticPath } from '@/utils/getCustomStaticPath';
                  
export const meta = {
  title: 'Query transfers',
  description: "Learn how to query enqueued transfers from the local database using Amplify's storage category.",
  platforms: ['android']
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}
          


import android0 from '/src/fragments/lib/storage/android/query-transfers.mdx';

<Fragments fragments={{ android: android0 }} />
