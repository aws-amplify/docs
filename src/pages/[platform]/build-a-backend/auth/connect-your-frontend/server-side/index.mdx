import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Server-side auth',
  description:
    'Learn how to use Amplify Auth on the server',
  platforms: [
    // 'angular',
    // 'javascript',
    // 'nextjs',
    // 'react',
    // 'vue'
  ]
};

export function getStaticPaths() {
  return getCustomStaticPath(meta.platforms);
}

export function getStaticProps() {
  return {
    props: {
      meta
    }
  };
}

{/* concepts for server-side auth, how does it work? how does it change how I interact with auth? */}
{/* cookies, and how this changes how you interact with auth */}
{/* protecting frontend resources with auth */}
{/* "admin only" route example (e.g. a dashboard page) */}

## Cookie storage for tokens

## Session validation
