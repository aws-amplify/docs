import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Customize auth',
  description:
    'Learn how to configure your Amplify Auth resource',
  platforms: [
    'android',
    'angular',
    'flutter',
    'javascript',
    'nextjs',
    'react',
    'react-native',
    'swift',
    'vue'
  ]
};

export function getStaticPaths() {
  return getCustomStaticPath(meta.platforms);
}

export function getStaticProps() {
  return {
    props: {
      meta
    }
  };
}

## Customize emails

{/* verification email (basic) */}
{/* verification email with react.email/jsx.email */}

## User attributes

{/* what standard attributes are */}
{/* callout on required attributes and how they cannot be changed but can be overridden by admins */}

## User groups

{/* how to set */}

## Customize authentication flows

{/* captcha */}
