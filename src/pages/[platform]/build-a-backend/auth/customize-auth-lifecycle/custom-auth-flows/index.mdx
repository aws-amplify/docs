import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Custom auth flows',
  description: 'Learn how to customize the authentication flows to support additional challenges such as reCAPTCHA',
  platforms: [
    // 'android',
    'angular',
    // 'flutter',
    'javascript',
    'nextjs',
    'react',
    // 'react-native',
    // 'swift',
    'vue'
  ]
};

export function getStaticPaths() {
  return getCustomStaticPath(meta.platforms);
}

export function getStaticProps() {
  return {
    props: {
      meta,
    }
  };
}

{/* reCAPTCHA */}
{/* passwordless? */}
