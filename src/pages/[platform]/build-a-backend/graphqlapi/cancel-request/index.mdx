import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Cancel API requests',
  description:
    "Learn more about how to cancel query or mutation requests with Amplify's GraphQL API category",
  platforms: ['javascript', 'react-native','angular','nextjs','react','vue'] 
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import js0 from '/src/fragments/lib/graphqlapi/js/cancel-request.mdx';

<Fragments fragments={{  javascript: js0 , angular: js0, nextjs: js0, react: js0, vue: js0 }} />

import reactnative0 from '/src/fragments/lib/graphqlapi/js/cancel-request.mdx';

<Fragments fragments={{ 'react-native': reactnative0 }} />
