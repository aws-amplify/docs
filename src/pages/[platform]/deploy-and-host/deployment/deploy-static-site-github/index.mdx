import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Deploy a static site from GitHub',
  description: 'How to deploy a static site to Amplify Hosting using a Git repo.',
  platforms: [
    'javascript',
    'angular',
    'nextjs',
    'react',
    'vue'
  ],
  canonicalObjects: [
    {
      platforms: [
        'react',
        'javascript',
        'nextjs',
        'vue',
        'angular'
      ],
      canonicalPath: '/javascript/deploy-and-host/deployment/deploy-static-site-github/'
    }
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import js0 from '/src/fragments/guides/hosting/git-based-deployments.mdx';

<Fragments
  fragments={{
    javascript: js0,
    angular: js0,
    nextjs: js0,
    react: js0,
    vue: js0
  }}
/>
