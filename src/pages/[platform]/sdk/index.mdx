import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'AWS Mobile SDK - Overview',
  description:
    'The AWS Mobile SDK enables you to build mobile apps by providing simplified APIs for AWS services such as Amazon Cognito, Amazon S3, AWS AppSync, and more.',
  disableTOC: 'true',
  platforms: ['swift', 'android'],
  hideChildrenOnBase: 'true'
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import ios0 from '/src/fragments/lib/ios-sdk.mdx';

<Fragments fragments={{ swift: ios0 }} />

import android1 from '/src/fragments/lib/android-sdk.mdx';

<Fragments fragments={{ android: android1 }} />
