import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'UI Builder migration',
  description: 'Migrate from Amplify Studio UI Builder to the Amplify UI Builder Figma Plugin for Gen 2.',
  platforms: [
    'android',
    'angular',
    'flutter',
    'javascript',
    'nextjs',
    'react',
    'react-native',
    'swift',
    'vue'
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

The Gen 1 UI Builder is part of Amplify Studio, the visual tool for managing Gen 1 applications. No such UI exists for Gen 2, but near-full feature parity can be achieved using the [Amplify UI Builder Figma Plugin](https://www.figma.com/community/plugin/1040722185526429545).

Prerequisites: design using Figma based on the [Amplify UI Kit](https://www.figma.com/community/file/1047600760128127424), and install the Amplify UI Builder Plugin.

| Feature | Gen 1 (Studio) | Gen 2 (Figma Plugin) |
|---|---|---|
| Adjusting theme and creating custom components | Supported | Supported |
| Connect data model to components before export | Supported (requires existing data model) | Not supported. Connect to Amplify Data as part of the engineering effort. |
| Exporting components into the project | Supported via UI Builder | Supported via Plugin |
