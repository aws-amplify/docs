import { getCustomStaticPath } from '@/utils/getCustomStaticPath';

export const meta = {
  title: 'Cancel requests',
  description: 'Learn how to cancel an in-flight get or put request from Storage.',
  platforms: [
    'javascript',
    'react-native',
    'angular',
    'nextjs',
    'react',
    'vue'
  ],
  canonicalObjects: [
    {
      platforms: [
        'javascript',
        'nextjs',
        'angular',
        'react',
        'vue'
      ],
      canonicalPath: '/javascript/prev/build-a-backend/storage/cancel-requests/'
    }
  ]
};

export const getStaticPaths = async () => {
  return getCustomStaticPath(meta.platforms);
};

export function getStaticProps(context) {
  return {
    props: {
      platform: context.params.platform,
      meta
    }
  };
}

import js0 from '/src/fragments/lib-v1/storage/js/cancel-requests.mdx';

<Fragments
  fragments={{
    javascript: js0,
    angular: js0,
    nextjs: js0,
    react: js0,
    vue: js0
  }}
/>

import reactnative0 from '/src/fragments/lib-v1/storage/js/cancel-requests.mdx';

<Fragments fragments={{ 'react-native': reactnative0 }} />
