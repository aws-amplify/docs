import meta from './meta.json';
import { getChildPageNodes } from '@/utils/getChildPageNodes';
import { Heading } from '@aws-amplify/ui-react';

export function getStaticProps() {
  return {
    props: {
      meta,
      ...meta,
      childPageNodes: getChildPageNodes(meta.route)
    }
  };
}

<Heading level={2} id="backend-tooling">Backend Tooling</Heading>

Amplify backend tooling helps you provision and manage AWS infrastructure. Choose a category below to get started.

<Overview childPageNodes={props.childPageNodes} />
