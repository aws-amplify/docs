import {css} from "emotion";

export const hostStyle = css`
  display: block;
  padding-bottom: 2rem;

  h3,
  h4 {
    display: block;
  }

  h3 {
    border-bottom: 0.125rem solid var(--border-color);
    padding: 1rem 0;
    margin-bottom: 1rem;
  }
`;
